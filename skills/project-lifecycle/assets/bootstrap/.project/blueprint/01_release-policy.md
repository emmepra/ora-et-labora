# Release Policy

- `dev` is the integration branch.
- `main` is the stable branch.
- Implementation PRs merge to `dev`.
- Release PRs merge from `dev` to `main`.
- Release PRs should summarize included work, release checks, and rollback notes.
