interface:
  display_name: "Ora et Labora"
  short_description: "Issue-to-release workflow for repo work"
  default_prompt: "Use $ora-et-labora to choose the right Ora et Labora workflow skill and locate the suite templates or scripts."
policy:
  allow_implicit_invocation: false
