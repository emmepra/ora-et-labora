interface:
  display_name: "Blueprint Guard"
  short_description: "Run blueprint fit checks before implementation"
  default_prompt: "Use $blueprint-guard to evaluate this issue against the project blueprint before coding."
